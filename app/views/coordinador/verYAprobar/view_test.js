'use strict';

describe('myApp.coordinadorVerYAprobar module', function() {

  beforeEach(module('myApp.coordinadorVerYAprobar'));

  describe('coordinadorVerYAprobar controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var $scope = {};
      var homeCtrl = $controller('CoordinadorVerYAprobarCtrl', {$scope: $scope});
      expect(homeCtrl).toBeDefined();
    }));

  });
});
