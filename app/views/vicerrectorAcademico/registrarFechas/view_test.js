'use strict';

describe('myApp.vicerrectorAcademicoRegistrarFechas module', function() {

  beforeEach(module('myApp.vicerrectorAcademicoRegistrarFechas'));

  describe('vicerrectorAcademicoRegistrarFechas controller', function() {

    it('should ....', inject(function($controller) {
      //spec body
      var $scope = {};
      var homeCtrl = $controller('VicerrectorAcademicoRegistrarFechasCtrl', {
        $scope: $scope
      });
      expect(homeCtrl).toBeDefined();
    }));

  });
});
